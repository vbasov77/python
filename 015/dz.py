# 30). Вычислить число π c заданной точностью d
# Пример:
# при d = 0.001,π = 3.141 10^(-1)≤d≤10^(-10)


pi = 0
s = 1
d = input('Введите число d: ')

for i in range(1000000):
    if i % 2 == 0:
        pi += 4 / s
    else:
        pi -= 4 / s
    s += 2

print(f'π =  {str(pi)[:len(d)]}')